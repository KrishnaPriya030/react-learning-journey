// Constant variables
const name = "Krishna";
const age = 22;

// Variable that can be changed later
let subject = "java";

// Display basic information
console.log(name);
console.log(age);
console.log(subject);

// Array of numbers
let numbers = [1, 2, 3, 4];

// Print each element in the array
for (let i = 0; i < numbers.length; i++) {
    console.log(numbers[i]);
}

// Arrow function that returns the friend's name
const friends = (name, age) => name;

// Function call
console.log(friends("kripa", 20));
